# Rsyslog istemci yapılandırması - tüm logları TCP ile SIEM sunucusuna gönderir
# Dosyayı hedef sunucuda /etc/rsyslog.d/60-siem.conf olarak koyun ve servisi yeniden başlatın.

*.* @@SIEM_HOST_IP:5514

# Eğer sadece auth.log göndermek isterseniz:
#auth,authpriv.* @@SIEM_HOST_IP:5514
