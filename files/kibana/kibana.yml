# Kibana (dışa açık)
server.host: "0.0.0.0"
server.port: 5601

# ES bağlantısı
elasticsearch.hosts: ["https://localhost:9200"]

# CA yolu script tarafından sabitlenir:
# elasticsearch.ssl.certificateAuthorities: ["/etc/kibana/certs/ca.crt"]

# Uyarıları susturmak için script rastgele encryption key'leri ekler:
# xpack.security.encryptionKey: "<random>"
# xpack.encryptedSavedObjects.encryptionKey: "<random>"
# xpack.reporting.encryptionKey: "<random>"

# Giriş ekranında basit tema (opsiyonel)
status.allowAnonymous: false